<%= render "users/post", user: @user, book: @book %>

<h1>users</h1>
<% @users.each do |user| %>
<div>
<%= attachment_image_tag user, :profile_image, :fill, 50, 50, format: "jpg", fallback: "no_image.jpg", size: "50x50" %>
<%= user.name %>
<%= link_to "show", user_path(user.id) %>
</div>
<% end %>
