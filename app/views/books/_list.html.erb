<h1>Books</h1>
<% books.each do |book| %>
<div>
  <%= attachment_image_tag book.user, :profile_image, :fill, 50, 50, format: "jpg", fallback: "no_image.jpg", size: "50x50" %>
  <%= link_to book_path(book.id) do %>
  <%= book.title %>
  <% end %>
  <%= book.body %>
</div>
<% end %>